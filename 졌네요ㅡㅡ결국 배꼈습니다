function solution(x) {
    var splited = String(x).split('');
    var sum = 0;
    for (var i = 0; i<splited.length; i++){
        sum += Number(splited[i]);
    }
    if( x % sum === 0 ){
        return true;
    }else{
        return false;
    }
}
