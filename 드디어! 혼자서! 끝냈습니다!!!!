let n = 5
let lost = [2, 4]
let reserve = [1, 3, 5]

function solution (n, lost, reserve) {
  const newArr = lost.concat(reserve)
  for(i = 0; i <= n; i++)
   return newArr.length
  }
console.log(solution(n, lost, reserve))
